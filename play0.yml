---
- name: Create a user on Linux
  hosts: localhost
  become: yes

  tasks:
    - name: Create user
      user:
        name: ansible_js_02
        password: "{{ 'Password123' | password_hash('sha512') }}"
        state: present
